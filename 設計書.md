
これはFirefoxの拡張機能です。

ニュータブを提供します。

最初はブックマークのルートが表示されている。

フォルダだったらフォルダアイコンが表示
URLだったらURLに相当するOGタイトルと、OGP画像を表示する。

この２種は同じカードで、Pinterestのタイリング表示を模していて、下にスクロールすれば（ビューエリアに入った時に）画像を表示する。

フォルダをクリックされたら、そのフォルダ内で同様に展開する。

仮想フォルダは、一番上に、「上層に戻る」リンクが有る。
